eureka:
  client:
    serviceUrl:
      defaultZone: ${eureka-url:http://localhost:8761/eureka/}
  instance:
    virtual-host-name: samplenodejs
    lease-renewal-interval-in-seconds: ${eureka-interval:5}
    prefer-ip-address: true
